module ReservationStation(
	input clock,
	/*ALU OP*/
	/*RX*/
	/*RY*/
	/*CDB*/
);

// Instanciar uma matriz para cada coluna da RS

// Como a RS vai saber se a instrução recebida tem os operandos prontos ou não?

// Percorrer a RS para realizar a execução das instruções
// A instrução que estiver na posição mais alta e possuir os operandos prontos será executada primeiro